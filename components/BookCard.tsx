import React from 'react'

const BookCard = ({ title, author, coverURL, slug }: { title: string, author: string, coverURL: string, slug: string }) => {
    return (
        <div>BookCard</div>
    )
}

export default BookCard